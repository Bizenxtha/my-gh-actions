name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: print greeting
        run: |
          echo "Hello Bijay!"
          echo "Welcome to your Github Actions first YAML file.
          
      - name: print goodbye
        run: echo "Done -Good Bye 👋"
